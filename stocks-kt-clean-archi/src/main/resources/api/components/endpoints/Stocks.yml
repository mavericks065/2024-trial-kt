get:
  tags:
    - Stock
  operationId: getStocks
  description: Retrieves a list of stocks.
  parameters:
    - in: query
      name: size
      description: Size.
      schema:
        type: number
      required: true
  responses:
    200:
      description: Successful extraction of records
      content:
        application/json:
          schema:
            $ref: "../schemas/Stock.yml"
